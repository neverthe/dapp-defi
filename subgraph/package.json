{
  "name": "defi-subgraph",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ defi-swap-sepolia",
    "create-local": "graph create --node http://localhost:8020/ defi-swap-sepolia",
    "remove-local": "graph remove --node http://localhost:8020/ defi-swap-sepolia",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 defi-swap-sepolia",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.96.0",
    "@graphprotocol/graph-ts": "^0.38.0",
    "matchstick-as": "^0.6.0"
  }
}